node_modules
.env
.env.local

# Sitio compilado: lo genera `npm run build` y lo rehace Vercel en cada subida.
# Tenerlo aquí sólo produce conflictos y peso muerto.
dist

# Fotos originales del cliente sin optimizar (cientos de MB). La tienda usa las
# de public/productos, que sí se suben. Estas son material de archivo:
# respaldarlas en Drive o en un disco, NO en GitHub.
originales

# Restos de herramientas
__pycache__
*.pyc
.vercel
.DS_Store
*.log
